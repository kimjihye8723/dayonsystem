const axios = require('axios'); require('dotenv').config(); (async => { const connectInfo = 'http://smartgate1001.cns-link.net:7002'; const now = new Date(); const startOfDay = new Date(now); startOfDay.setHours(0, 0, 0, 0); const pad = (n) => n.toString().padStart(2, '0'); const getFormattedTime = (date) => date.getFullYear() + '-' + pad(date.getMonth() + 1) + '-' + pad(date.getDate()) + 'T' + pad(date.getHours()) + ':' + pad(date.getMinutes()) + ':' + pad(date.getSeconds()); const startTime = getFormattedTime(startOfDay); const endTime = getFormattedTime(now); try { console.log('Requesting', startTime, endTime); const response = await axios.get(connectInfo + '/api/statistics/query', { params: { start: startTime, end: endTime, interval: 'by1day' }, auth: { username: process.env.CCTV_USER || 'admin', password: process.env.CCTV_PASSWORD || 'Tectree6767!' }, headers: { 'Connection': 'close' }, timeout: 60000 }); console.log(JSON.stringify(response.data, null, 2)); } catch (err) { console.error('Error:', err.message); if (err.response) console.error('Status:', err.response.status); } })();  
